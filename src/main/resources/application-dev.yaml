spring:
  datasource:
    url: jdbc:mysql://db:3306/sensor_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true
  thymeleaf:
    cache: false
logging:
  level:
    '[org.springframework.web]': DEBUG
    '[org.hibernate.SQL]': DEBUG
